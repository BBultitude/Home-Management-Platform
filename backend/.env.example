# Backend Environment Configuration (for local development only)
# In Docker, these are set via docker-compose.yml

ENVIRONMENT=development
DEBUG=true
DATABASE_URL=postgresql://homeuser@localhost:5432/homedb
